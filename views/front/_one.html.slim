div.col-xs-12.col-sm-12.col-md-5.col-lg-5
  = link_to(image_tag(app.logo_url, :class => "welcome-logo#{subclass}", :title => app.name), app.full_url, :target => '_blank')
  = link_to(app.name, app.full_url, :target => '_blank')